namespace OrbitHub.FileManagement.Services;

/// <summary>
/// Singleton store for file management data.
/// Currently serves as a placeholder — data is loaded from the database via FileManagementDbContext.
/// </summary>
public class FileStore
{
    // Future: add in-memory caching, mock data loading, or DB-backed operations here.
}