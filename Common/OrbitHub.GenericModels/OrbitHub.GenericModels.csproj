<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>OrbitHub.GenericModels</RootNamespace>
    <AssemblyName>OrbitHub.GenericModels</AssemblyName>
    <Description>Shared domain models and enums for all OrbitHub projects. Zero external dependencies.</Description>
  </PropertyGroup>

</Project>